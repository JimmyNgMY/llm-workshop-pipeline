name: Build Multiroute Image
on:
  push:
    paths:
      - 'llm-multiroute/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: ./llm-multiroute
          push: true
          # Note: Uses settings from main.py and config.py
          tags: ghcr.io/${{ github.repository }}/llm-multiroute:latest